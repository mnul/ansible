---

- hosts: all
  become: true
  tasks:

  - name: update cache
    apt:
      update_cache: yes

  
  - name: full upgrade
    apt:
      upgrade: full

  - name: autoremove
    apt:
      autoremove: yes
    
  - name: clean apt cache
    apt:
      autoclean: yes
    
